using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace ViewModel.Extentions
{
    public static class DataBaseModelExtentions
    {
        //내부적으로 돌아가고 콜백이 딱히 필요가 없네?

        //콜백 해준다면 뭐기 있어야 할까?
    }

    /*public static void RegisterEventsOnEnable(this LoginViewModel vm)
    {
        DataBaseManager.Inst.RegisterLoginCallback(vm.);
    }

    public static void UnRegisterOnDisable(this LoginViewModel vm)
    {
        DataBaseManager.Inst.UnRegisterLevelUpCallback(vm.OnResponseLogin);
    }

    public static void OnResponseLogin(this LoginViewModel vm, int userId, int level)
    {

    }*/


}

